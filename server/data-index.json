---
---
[
{% capture dataDir %}{{'/data/'}}{% endcapture %}{%
for page in site.pages %}{%
	assign urlPrefix = page.url | slice: 0, 6 %}{%
	if urlPrefix == dataDir 
		%}	"{{ page.url }}",
{%
	endif %}{%
endfor %}{% 

for static_file in site.static_files %}{% 
	assign urlPrefix = static_file.path | slice: 0, 6 %}{%
	if urlPrefix == dataDir
		%}	"{{ static_file.path }}"{% 
		if forloop.last == false 
			%},{% 
		endif 
		%}
{%
	endif %}{% 
endfor 
%}]